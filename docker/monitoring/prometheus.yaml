global:
  scrape_interval:     15s
  evaluation_interval: 15s
  external_labels:
    monitor:     'store-tdr-hpz'
    environment: 'production'

scrape_configs:
  # prometh self monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
